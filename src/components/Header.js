import React from 'react';
import './header.css';
import Menu from './Menu';

const Header = () => {
  return (
    <div className="header" id="header">
      <Menu />
    </div>
  );
};

export default Header;
